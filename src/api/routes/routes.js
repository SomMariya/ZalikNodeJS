import { handler } from '../../utils';import { getWeather } from '../services';// import { weatherSchema } from '../scheme';export const routes = [  {    method: 'GET',    url: '/weather/:city',    handler: handler((req) => getWeather(req.params.city))  }];